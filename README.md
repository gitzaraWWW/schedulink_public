# schedulink

## Environment variables

This repository is safe to publish without local secret files.

Before running the app locally, copy the example files and fill in your own values:

```bash
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env
```

Never commit real `.env` files, Firebase service account JSON files, or provider API keys.
